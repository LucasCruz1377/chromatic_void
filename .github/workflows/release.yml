name: CD - Publicar Chromatic Void

on:
  push:
    tags:
      - "v*"

  workflow_dispatch:
    inputs:
      tag:
        description: "Tag existente que será publicada, por exemplo v0.6.0-beta.1"
        required: true
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref_name }}-${{ inputs.tag }}
  cancel-in-progress: false

jobs:
  preparar:
    name: Validar tag e projeto
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      tag: ${{ steps.versao.outputs.tag }}
      prerelease: ${{ steps.versao.outputs.prerelease }}

    steps:
      - name: Baixar o repositório
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Classificar versão
        id: versao
        shell: bash
        env:
          TAG_INFORMADA: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          if [[ ! "$TAG_INFORMADA" =~ ^v[0-9]+\.[0-9]+\.[0-9]+(-(alpha|beta|rc)\.[0-9]+)?$ ]]; then
            echo "Tag inválida: $TAG_INFORMADA"
            echo "Use vX.Y.Z, vX.Y.Z-alpha.N, vX.Y.Z-beta.N ou vX.Y.Z-rc.N."
            exit 1
          fi

          echo "tag=$TAG_INFORMADA" >> "$GITHUB_OUTPUT"
          if [[ "$TAG_INFORMADA" == *-* ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Instalar Godot 4.7
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.7.0
          use-dotnet: false
          include-templates: false

      - name: Verificar referências de recursos
        run: python3 Tests/validar_referencias.py

      - name: Importar projeto em modo headless
        run: godot --headless --audio-driver Dummy --path . --import --quit

      - name: Executar smoke tests
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t testes < <(find Tests -maxdepth 1 -name '*_smoke.tscn' -print | sort)
          if [[ ${#testes[@]} -eq 0 ]]; then
            echo "Nenhum smoke test foi encontrado."
            exit 1
          fi

          for teste in "${testes[@]}"; do
            echo "Executando $teste"
            timeout 180s godot \
              --headless \
              --audio-driver Dummy \
              --path . \
              "res://$teste"
          done

  exportar:
    name: Exportar ${{ matrix.nome }}
    needs: preparar
    runs-on: ubuntu-latest
    timeout-minutes: 20

    strategy:
      fail-fast: false
      matrix:
        include:
          - preset: Windows Desktop
            nome: Windows
            artefato: windows
            android: false
          - preset: Web
            nome: Web
            artefato: web
            android: false
          - preset: Android APK
            nome: Android APK
            artefato: android
            android: true

    steps:
      - name: Baixar a versão da tag
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.preparar.outputs.tag }}

      - name: Aplicar versão da tag aos executáveis
        env:
          TAG: ${{ needs.preparar.outputs.tag }}
          VERSION_CODE: ${{ github.run_number }}
        run: python3 Tests/aplicar_versao_release.py "$TAG" "$VERSION_CODE"

      - name: Preparar Java 17 para Android
        if: matrix.android == true && vars.ANDROID_RELEASE_ENABLED == 'true'
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Preparar Android SDK
        if: matrix.android == true && vars.ANDROID_RELEASE_ENABLED == 'true'
        uses: android-actions/setup-android@v4

      - name: Instalar componentes Android exigidos pelo Godot
        if: matrix.android == true && vars.ANDROID_RELEASE_ENABLED == 'true'
        shell: bash
        run: sdkmanager "platform-tools" "build-tools;35.0.1" "platforms;android-35"

      - name: Preparar assinatura privada do APK
        if: matrix.android == true && vars.ANDROID_RELEASE_ENABLED == 'true'
        shell: bash
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        run: |
          set -euo pipefail
          if [[ -z "$ANDROID_KEYSTORE_BASE64" || -z "$ANDROID_KEY_ALIAS" || -z "$ANDROID_KEYSTORE_PASSWORD" ]]; then
            echo "Configure os três secrets ANDROID_KEYSTORE_BASE64, ANDROID_KEY_ALIAS e ANDROID_KEYSTORE_PASSWORD."
            exit 1
          fi
          mkdir -p "${RUNNER_TEMP}/android-signing"
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > "${RUNNER_TEMP}/android-signing/chromatic-void.keystore"
          keytool -list -keystore "${RUNNER_TEMP}/android-signing/chromatic-void.keystore" -storepass "$ANDROID_KEYSTORE_PASSWORD" -alias "$ANDROID_KEY_ALIAS" >/dev/null

      - name: Exportar ${{ matrix.nome }} com Godot
        if: matrix.android != true || vars.ANDROID_RELEASE_ENABLED == 'true'
        id: export
        uses: firebelley/godot-export@v8.0.0
        env:
          GODOT_ANDROID_KEYSTORE_RELEASE_PATH: ${{ runner.temp }}/android-signing/chromatic-void.keystore
          GODOT_ANDROID_KEYSTORE_RELEASE_USER: ${{ secrets.ANDROID_KEY_ALIAS }}
          GODOT_ANDROID_KEYSTORE_RELEASE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        with:
          godot_executable_download_url: https://github.com/godotengine/godot-builds/releases/download/4.7-stable/Godot_v4.7-stable_linux.x86_64.zip
          godot_export_templates_download_url: https://github.com/godotengine/godot-builds/releases/download/4.7-stable/Godot_v4.7-stable_export_templates.tpz
          relative_project_path: ./
          presets_to_export: ${{ matrix.preset }}
          export_debug: false
          archive_output: true
          cache: true

      - name: Guardar build para publicação
        if: matrix.android != true
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.artefato }}
          path: ${{ steps.export.outputs.archive_directory }}
          if-no-files-found: error
          retention-days: 7

      - name: Guardar APK para publicação
        if: matrix.android == true && vars.ANDROID_RELEASE_ENABLED == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.artefato }}
          path: ${{ steps.export.outputs.build_directory }}/**/*.apk
          if-no-files-found: error
          retention-days: 7

  publicar:
    name: Criar GitHub Release
    needs:
      - preparar
      - exportar
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Baixar o repositório na tag
        uses: actions/checkout@v5
        with:
          ref: ${{ needs.preparar.outputs.tag }}
          fetch-depth: 0

      - name: Baixar builds aprovados
        uses: actions/download-artifact@v4
        with:
          pattern: release-*
          path: dist
          merge-multiple: true

      - name: Organizar arquivos e gerar checksums
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-assets
          while IFS= read -r -d '' arquivo; do
            destino="release-assets/$(basename "$arquivo")"
            if [[ -e "$destino" ]]; then
              echo "Dois builds geraram o mesmo nome: $destino"
              exit 1
            fi
            cp "$arquivo" "$destino"
          done < <(find dist -type f -print0)

          if [[ -z "$(find release-assets -maxdepth 1 -type f -print -quit)" ]]; then
            echo "Nenhum build foi baixado para a Release."
            exit 1
          fi

          cd release-assets
          sha256sum ./* > SHA256SUMS.txt

      - name: Criar Release estável
        if: needs.preparar.outputs.prerelease == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.preparar.outputs.tag }}
        run: |
          gh release create "$TAG" release-assets/* \
            --title "Chromatic Void $TAG" \
            --generate-notes \
            --verify-tag \
            --latest

      - name: Criar pré-lançamento
        if: needs.preparar.outputs.prerelease == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TAG: ${{ needs.preparar.outputs.tag }}
        run: |
          gh release create "$TAG" release-assets/* \
            --title "Chromatic Void $TAG" \
            --generate-notes \
            --verify-tag \
            --prerelease \
            --latest=false
